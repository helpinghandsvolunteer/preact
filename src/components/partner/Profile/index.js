import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style';

const Profile = () => (
	<div class={style.eeprofile_container}>
		<p>Profile</p>
	</div>
);

export default Profile;
