import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style';

const Network = () => (
	<div class={style.eenetwork_container}>
		<p>Network</p>
	</div>
);

export default Network;
