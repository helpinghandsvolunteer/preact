import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style';

const Settings = () => (
	<div class={style.eesettings_container}>
		<p>Settings</p>
	</div>
);

export default Settings;
