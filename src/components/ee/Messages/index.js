import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style';

const Messages = () => (
	<div class={style.eemessages_container}>
		<p>Messages</p>
	</div>
);

export default Messages;
