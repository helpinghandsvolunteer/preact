import { h } from 'preact';
import style from './style';

const EmployeeHome = () => (
	<div class={style.employee}>
		<h1>Employee Home</h1>
		<p>This is the Employee page component.</p>
	</div>
);

export default EmployeeHome;
